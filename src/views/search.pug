extends layout
include mixins/post

block content 
    form(method="GET", class="search__form")
        input(type="text", placeholder="검색어 입력", name="keyword")
        button
            i.fas.fa-search

    div 
        each post in posts 
            +post(post)
        else 
            span.not_found 포스트를 찾을 수 없습니다.

block script 
    script(src="/assets/js/search.js")