extends layout
include mixins/post

block content 
    form(method="GET")
        input(type="text", placeholder="검색어 입력", name="keyword")
        input(type="submit", value="OK")

    div 
        each post in posts 
            +post(post)
