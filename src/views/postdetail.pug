extends layout.pug

block header
    h2=post.title

block content 
    div 
        p=post.content 
        small=post.creationDate
    a(href=`${post.id}/edit`) 포스트 수정
    br
    a(href=`${post.id}/delete`) 포스트 삭제