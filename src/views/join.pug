extends layout 

block content 
    form(method="POST")
        input(name="name", type="text", required)
        input(name="email", type="email", required)
        input(name="password", type="password", required)
        input(name="aboutuser", type="text", required)
        input(type="submit", value="확인")